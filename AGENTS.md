# GitInsight AI — Agent Rules

> [!IMPORTANT]
> Keep the branch in a working state. Avoid rewriting published git history
> (force pushing, rebasing/amending/squashing commits that are already pushed).
